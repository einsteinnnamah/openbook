import Layout from "@/components/layouts/Layout";
import React from "react";

const index = () => {
  return (
    <Layout active="Swap">
      <div>swap</div>
    </Layout>
  );
};

export default index;
